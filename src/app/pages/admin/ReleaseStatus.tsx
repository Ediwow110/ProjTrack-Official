import { CheckCircle2, Clock3, RefreshCcw, ShieldAlert } from "lucide-react";
import { adminService } from "../../lib/api/services";
import { useAsyncData } from "../../lib/hooks/useAsyncData"; function iconFor(status: string) { if (status === "done") return <CheckCircle2 size={18} className="text-emerald-700" />; if (status === "in_progress") return <Clock3 size={18} className="text-amber-700" />; return <ShieldAlert size={18} className="text-rose-700" />;
} function tone(status: string) { if (status === "done") return "bg-emerald-50 border-emerald-200"; if (status === "in_progress") return "bg-amber-50 border-amber-200"; return "bg-rose-50 border-rose-200";
} export default function AdminReleaseStatus() { const { data, loading, reload } = useAsyncData(() => adminService.getReleaseStatus(), []); const rows = data ?? []; return ( <div className="p-6 max-w-6xl mx-auto space-y-6"> <div className={`flex items-start justify-between gap-4 ${loading ? "opacity-95" : ""}`}> <div> <h1 className={`text-slate-900 font-bold ${loading ? "opacity-80" : ""}`} style={{ fontSize: "1.3rem", letterSpacing: "-0.02em" }}>Release Status</h1> <p className={`text-slate-400 text-sm mt-0.5 ${loading ? "opacity-80" : ""}`}> Current release readiness across the core system. </p><p className={`text-slate-400 text-xs mt-1 ${loading ? "opacity-80" : ""}`}>{loading ? "Loading release status…" : `${rows.length} release area${rows.length === 1 ? "" : "s"}`}</p><p className={`text-slate-400 text-xs mt-1 ${loading ? "opacity-80" : ""}`}>{loading ? "Updating release readiness…" : "Review the current release blockers and completed areas."}</p> </div> <button disabled={loading} onClick={reload} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50 disabled:opacity-50" > <RefreshCcw size={14} /> Refresh </button> </div> <div className={`rounded-xl border border-blue-100 bg-blue-50 px-4 py-3 text-xs font-medium text-blue-700 ${loading ? "opacity-80" : ""}`}> Review completed areas and remaining release blockers. </div>           <div className={`grid grid-cols-1 md:grid-cols-2 gap-4 ${loading ? "opacity-80" : ""}`}> {loading && rows.length === 0 ? Array.from({ length: 6 }).map((_, i) => ( <div key={i} className="rounded-xl border border-slate-100 bg-white p-4 shadow-sm"> <div className="h-20 animate-pulse rounded-lg bg-slate-100" /> </div> )) : rows.map((row) => ( <div key={row.area} className={`rounded-xl border p-4 shadow-sm ${tone(row.status)} ${loading ? "opacity-80" : ""}`}> <div className={`flex items-start gap-3 ${loading ? "opacity-95" : ""}`}> <div className={`mt-0.5 ${loading ? "opacity-80" : ""}`}>{iconFor(row.status)}</div> <div className="space-y-1"> <h3 className={`text-sm font-bold text-slate-800 ${loading ? "opacity-80" : ""}`}>{row.area}</h3> <p className={`text-xs uppercase tracking-wide text-slate-500 ${loading ? "opacity-80" : ""}`}>{row.status.replace("_", " ")}</p> <p className={`text-sm text-slate-600 ${loading ? "opacity-80" : ""}`}>{row.detail}</p> </div> </div> </div> ))} </div> </div> );
}
