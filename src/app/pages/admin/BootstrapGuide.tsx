import { CheckCircle2, Clock3, RefreshCcw, Wrench } from "lucide-react";
import { adminService } from "../../lib/api/services";
import { useAsyncData } from "../../lib/hooks/useAsyncData"; function tone(status: string) { if (status === "ready") return "bg-emerald-50 border-emerald-200 text-emerald-700"; if (status === "action_needed") return "bg-rose-50 border-rose-200 text-rose-700"; return "bg-amber-50 border-amber-200 text-amber-700";
} function icon(status: string) { if (status === "ready") return <CheckCircle2 size={16} className="text-emerald-700" />; if (status === "action_needed") return <Wrench size={16} className="text-rose-700" />; return <Clock3 size={16} className="text-amber-700" />;
} export default function AdminBootstrapGuide() { const { data, loading, reload } = useAsyncData(() => adminService.getBootstrapGuide(), []); const steps = data ?? []; return ( <div className="p-6 max-w-6xl mx-auto space-y-6"> <div className="flex items-start justify-between gap-4"> <div> <h1 className={`text-slate-900 font-bold ${loading ? "opacity-80" : ""}`} style={{ fontSize: "1.3rem", letterSpacing: "-0.02em" }}>Deployment Checklist</h1> <p className={`text-slate-400 text-sm mt-0.5 ${loading ? "opacity-80" : ""}`}> Deployment checklist overview. </p><p className="text-slate-400 text-xs mt-1">{loading ? "Loading deployment checklist…" : `${steps.length} deployment step${steps.length === 1 ? "" : "s"}`}</p> </div> <button disabled={loading} onClick={reload} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50 disabled:opacity-50" > <RefreshCcw size={14} /> Refresh </button> </div> <div className={`rounded-xl border border-blue-100 bg-blue-50 px-4 py-3 text-xs font-medium text-blue-700 ${loading ? "opacity-80" : ""}`}> Review current deployment requirements and blockers. </div>         <div className={`grid grid-cols-1 md:grid-cols-2 gap-4 ${loading ? "opacity-80" : ""}`}>  {loading && steps.length === 0 ? Array.from({ length: 5 }).map((_, i) => ( <div key={i} className="rounded-xl border border-slate-100 bg-white p-4 shadow-sm"> <div className="h-24 animate-pulse rounded-lg bg-slate-100" /> </div> )) : steps.map((step) => ( <div key={step.title} className={`rounded-xl border p-4 shadow-sm ${tone(step.status)} ${loading ? "opacity-80" : ""}`}> <div className="flex items-start gap-3"> <div className="mt-0.5">{icon(step.status)}</div> <div className="space-y-1"> <h3 className={`text-sm font-bold text-slate-800 ${loading ? "opacity-80" : ""}`}>{step.title}</h3> <p className="text-xs uppercase tracking-wide text-slate-500">{step.status.replace("_", " ")}</p> <p className="text-sm text-slate-600">{step.detail}</p> </div> </div> </div> ))} </div> </div> );
}
