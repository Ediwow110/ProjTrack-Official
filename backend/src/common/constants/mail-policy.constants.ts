export const ACCOUNT_ACTION_TOKEN_TTL_MS = {
  PASSWORD_RESET: 60 * 60 * 1000,
  ACCOUNT_ACTIVATION: 24 * 60 * 60 * 1000,
} as const;

export const MAIL_IDEMPOTENCY_WINDOWS_MS = {
  PASSWORD_RESET: 15 * 60 * 1000,
  ACCOUNT_ACTIVATION: 15 * 60 * 1000,
  INVITATION: 60 * 60 * 1000,
} as const;

export const MAIL_RETRY_DELAYS_MS = [60_000, 300_000, 900_000, 3_600_000] as const;

export const MAIL_SENDER_CONFIRMATION_RETRY_DELAYS_MS = [
  30 * 60_000,
  60 * 60_000,
  2 * 60 * 60_000,
  4 * 60 * 60_000,
] as const;

export const DEFAULT_MAIL_MAX_ATTEMPTS = 5;

export const MAIL_LIMIT_DEFAULTS = {
  MONTHLY_LIMIT: 80_000,
  DAILY_SAFETY_LIMIT: 2_000,
  BULK_PER_MIN: 20,
  TX_PER_MIN: 60,
} as const;

export const MAIL_LIMIT_ENV_KEYS = {
  MONTHLY_LIMIT: ['MAILRELAY_MONTHLY_LIMIT'],
  DAILY_SAFETY_LIMIT: ['MAILRELAY_DAILY_SAFETY_LIMIT'],
  BULK_PER_MIN: ['MAILRELAY_BULK_PER_MIN', 'MAIL_BULK_PER_MIN'],
  TX_PER_MIN: ['MAILRELAY_TX_PER_MIN', 'MAIL_TX_PER_MIN'],
} as const;
